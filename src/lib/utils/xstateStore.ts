// An attempt to write my own XState machine-based Svelte store
// to replace useMachine and to persist state locally.
import { browser } from "$app/environment";

// export function holdMyState(machine: any, state);
